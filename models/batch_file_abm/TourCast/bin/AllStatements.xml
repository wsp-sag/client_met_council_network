<?xml version="1.0" encoding="utf-8" ?>
<sqlMap namespace="DBMapper" xmlns="http://ibatis.apache.org/mapping"
xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">

  <statements>
<!--
  this works:
    <select id="Select" parameterClass="string" resultClass="HashTable">
      $value$
    </select>

this works too:
-->
<select id="Select" parameterClass="string" resultClass="System.Collections.Generic.List`1[System.Object]">
      $value$
    </select>

<!-- doesn't work : "Unable to optimize create instance. Cause : Could not find public constructor matching specified arguments for type "Object[]".
    i.e. no parameterless ctor for object[]; could wrap it in a class
    <select id="Select" parameterClass="string" resultClass="System.Object[]">
      $value$
    </select>
-->
    <!-- MySql -->
    <!--        
    <insert id="InsertWithAutoGeneratedKey" parameterClass="string">
      <selectKey resultClass="int" type="post" property="id" >
        select LAST_INSERT_ID() as value
      </selectKey>
      #value#
    </insert>

    <insert id="InsertWithSuppliedKey" parameterClass="string">
      #value#
    </insert>

    <update id="Update" parameterClass="string">
      #value#
    </update>

    <delete id="TIPDelete" parameterClass="string">
      #value#
    </delete>
-->
  </statements>
</sqlMap>
